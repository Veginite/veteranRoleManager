1. add .env to root and add: DISCORD_TOKEN=""
2. replace role IDs in roles.xml with the veteran role IDs in the Conflux Discord server
3. in main.py - on_message(), replace the relevant IDs
4. in responses.py - replace the channel ID in clause "addleague" to the bot channel reserved for staff